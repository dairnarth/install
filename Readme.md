# Install

A python script to install Arch Linux based on the [archinstall library](https://github.com/archlinux/archinstall).

It asks only for a `hostname`, `username`, and `password`, then sets up a btrfs filesystem and installs the OS, program list and my dotfiles repository.
