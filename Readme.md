# Install

A python script to install Arch Linux based on the [archinstall library](https://github.com/archlinux/archinstall).

It asks only for a `hostname`, `username`, and `password`, then sets up a btrfs filesystem, installs the OS, some programs and, optionally, my dotfiles repository.
